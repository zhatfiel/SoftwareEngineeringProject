<h1>Listing articles</h1><br>
<%= link_to 'Home', welcome_index_path %>
<%= link_to 'New Article', new_article_path %>
<%= link_to 'Index', articles_path %>
<%= link_to 'SortA', articles_sortA_path %>
<%= link_to 'SortZ', articles_sortZ_path %>
<br><br>
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th>Date</th>
    <th>Author</th>
  </tr>
  
  
  <% @articlesSortZ.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= article.updated_at %></td>
      <td><% if article.user %>
        <%= article.user.name %>
        <% end %>
      </td>
      
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete',
                                article_path(article),
                                method: :delete,
                                data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
</table>